name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        env:
          # This matches your repository name for asset pathing
          BASE_PATH: "/devhub-terminal/"
          VITE_BASE_PATH: "/devhub-terminal/"
        run: bun run build

      - name: Prepare Static Files for GitHub Pages
        run: |
          # 1. TanStack Start puts static files in dist/client. 
          # We need them in the root of dist for GitHub Pages to see them.
          if [ -d dist/client ]; then
            echo "Relocating client files to root..."
            cp -r dist/client/* dist/
          fi
          
          # 2. Create the 404.html from index.html to handle SPA routing.
          # We use 'cp' instead of a redirect script to avoid infinite loops.
          if [ -f dist/index.html ]; then
            cp dist/index.html dist/404.html
          fi
          
          # 3. Bypass Jekyll processing (crucial for folders starting with underscores)
          touch dist/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload the flattened dist folder
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
